import React, {useEffect, useRef} from 'react';
import classnames from 'classnames';

export function Previewer(): JSX.Element {
    const cls = classnames('rp-previewer');

    return (
        <div className={cls}>
        </div>
    );
}